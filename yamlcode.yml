---
- hosts: webserver
  become: yes
  tasks:
  - name: i want to install webserver on RHEL
    yum: 
      name: "{{webserverfor redhat}}"
      state: present
  - name: start the httpd service after installing
    service:
      name: "{{webserverfor redhat}}"
      state: started
 